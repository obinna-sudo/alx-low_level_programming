#include <stdio.h>

/**
 * main - Program that prints numbers from 0 to 9
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)

		printf("%d\n", num);
	printf("\n");

	return (0);
}
